query getProductDetail ($sku:String!){
  product(sku:$sku) {
    data {
      id
      name
      sku
      images {
        src
        position
      }
      vendors {
        vendor_id
        vendor_sku
      }
    }
  }
}
