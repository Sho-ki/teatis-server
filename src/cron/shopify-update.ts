
const shopifyUpdate = () => {
//   await NestFactory.createApplicationContext(WebhookEventModule);
//   const appService = standAloneApp.get(WebhookEventService);
//   appService.checkUpdateOrderWebhook();
//   await standAloneApp.close();

  // eslint-disable-next-line no-console
  console.log('hello world');
};

shopifyUpdate();
